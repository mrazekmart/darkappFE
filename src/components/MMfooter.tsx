import React from 'react';

const Footer = () => {
    return (
        <footer className="text-center mt-5">
            <p>&copy; {new Date().getFullYear()} Science Web. All rights reserved.</p>
        </footer>
    );
};

export default Footer;